// Twitter/X card image. Identical to the site-wide Open Graph card — re-exported
// so `twitter:image` is emitted and resolved as an absolute URL against
// `metadataBase` (the production origin).
export { default, alt, size, contentType } from "./opengraph-image";
