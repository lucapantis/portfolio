// Twitter/X card image for the ReturnOps case study. Identical to the route's
// Open Graph card — re-exported so `twitter:image` is emitted and resolved as an
// absolute URL against `metadataBase` (the production origin).
export { default, alt, size, contentType } from "./opengraph-image";
